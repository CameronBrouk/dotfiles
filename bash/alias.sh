# LS Aliases
alias ls="ls -a --sort=extension --width=80 --color=auto"
alias ll='ls -alF'
alias la='ls -A'
alias list='ls'

# Faster git typing
alias g="git"

# Making sure I am always using nvim
alias vi="nvim"
alias vim="nvim"
